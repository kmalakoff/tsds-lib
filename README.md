# tsds-lib

Internal configuration and command utilities used by `ts-dev-stack` and its
command packages.

Install and configure `ts-dev-stack` rather than using this package directly.
See the [ts-dev-stack documentation](https://www.npmjs.com/package/ts-dev-stack).
